// Point this at wherever the Spring Boot backend is actually running.
// Local development default matches `mvnw spring-boot:run` on its default port.
window.API_BASE_URL = "http://localhost:8080/api";
